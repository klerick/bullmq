{
  "name": "bullmq-go-interop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Node BullMQ pinned to the rawScripts version (BullMQ v6), used by the Go port's cross-runtime interop tests.",
  "dependencies": {
    "bullmq": "6.0.10"
  }
}
